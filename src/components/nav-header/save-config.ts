import type { SuperGlobuleConfig } from "$lib/types";

export const isModified = (superConfig: SuperGlobuleConfig) => {
  return true;
}

export const isSaved = (superConfig: SuperGlobuleConfig) => {
  return true;
}